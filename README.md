# AOC for future aoc events

To create a new day: `cargo generate --path ./template --name day-<num>`

To run a part: `cargo run -p <day> --bin part#`
To test a part: `cargo test -p <day> --bin part#`
